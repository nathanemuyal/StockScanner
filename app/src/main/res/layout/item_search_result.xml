<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvItemSku"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/tvItemDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp" />

            <!-- Only shown when this list has more than one row for the same
                 מקט at the same מיקום (different ברקוד each) — otherwise the
                 barcode is redundant clutter, so it stays hidden. -->
            <TextView
                android:id="@+id/tvItemBarcode"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:textColor="@color/color_text_secondary"
                android:visibility="gone" />
        </LinearLayout>

        <!-- Only shown where the adapter is given a removal action (the
             shelf list on the main screen) — hidden everywhere else (e.g.
             the description-search results, which are select-only). Plain
             red glyph, no circle/box behind it — just a borderless ripple
             on touch, to keep the row uncluttered. -->
        <TextView
            android:id="@+id/btnRemoveFromLocation"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="הסר מהמיקום"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="−"
            android:textColor="@color/color_warning"
            android:textSize="28sp"
            android:textStyle="bold"
            android:visibility="gone" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_divider"
        android:layout_marginTop="12dp" />

</LinearLayout>
