# הוראות בנייה והתקנה — Stock Scanner

## דרישות מוקדמות
1. **Android Studio** גרסה עדכנית (Koala/2024.x ואילך מומלץ) — כולל JDK מובנה.
2. טלפון Android עם מצלמה, Android 7.0 (API 24) ומעלה.

## שלב 1 — פתיחת הפרויקט
1. פתחו את Android Studio.
2. `File → Open` ובחרו את התיקייה `BarcodeShorting` (תיקיית השורש של הפרויקט,
   זו שמכילה את `settings.gradle.kts`).
3. חכו ל־Gradle Sync. **אם מופיעה הודעה שחסר Gradle Wrapper JAR** (ייתכן כי
   קובץ הבינארי `gradle-wrapper.jar` לא נכלל בפרויקט שנוצר) — Android Studio
   בדרך כלל מציע לתקן זאת אוטומטית (כפתור "OK"/"Sync Now"). אם לא, אפשר גם:
   - `File → Settings → Build Tools → Gradle` ולבחור "Use Gradle from: 'wrapper' task"
     או "Use Gradle from: Specified location" ולהצביע על ה־Gradle המובנה ב-Android Studio.
   - לחלופין, אם מותקן Gradle במחשב, להריץ בטרמינל בתוך תיקיית הפרויקט:
     ```bash
     gradle wrapper --gradle-version 8.4
     ```
4. וודאו ש-Gradle Sync הסתיים בהצלחה (ללא שגיאות באדום בתחתית המסך).

## שלב 2 — הרצה על מכשיר אמיתי (מומלץ, כדי לבדוק מצלמה וסריקה)
1. בטלפון: `הגדרות → אודות הטלפון` ולחצו 7 פעמים על "מספר build" כדי להפעיל
   מצב מפתחים, ואז `הגדרות → אפשרויות מפתחים → ניפוי USB` (USB Debugging) — הפעילו.
2. חברו את הטלפון למחשב בכבל USB ואשרו את בקשת ניפוי ה-USB שתופיע בטלפון.
3. ב-Android Studio, בחרו את המכשיר שלכם מרשימת המכשירים למעלה, ולחצו ▶ Run.
4. האפליקציה תותקן ותופעל ישירות בטלפון.

## שלב 3 — בניית APK להתקנה ידנית
1. `Build → Build App Bundle(s) / APK(s) → Build APK(s)`.
2. בסיום, יופיע קישור "locate" — ה-APK יימצא ב:
   ```
   app/build/outputs/apk/debug/app-debug.apk
   ```
3. העבירו את הקובץ לטלפון (כבל USB, מייל לעצמכם, וואטסאפ וכו').
4. בטלפון: פתחו את קובץ ה-APK. אם מוצגת אזהרה "התקנת אפליקציות ממקורות
   לא ידועים" — אשרו זאת עבור האפליקציה שדרכה פתחתם את הקובץ (לדוגמה
   "קבצים" או הדפדפן). התקינו.

> לשימוש פנימי חד־פעמי, APK מסוג debug מספיק לחלוטין ואין צורך בחתימת release.

## שימוש באפליקציה
1. **בחר קובץ Excel** — בחרו את קובץ ה-`.xlsx` עם העמודות `מקט`, `תאור`,
   `ברקוד`, `מיקום` (בכל סדר עמודות — האפליקציה מזהה לפי שם הכותרת).
2. **סרוק מיקום** — סרקו את ה-QR שעל המדף.
3. **סרוק מוצר** — סרקו ברקוד מוצר; יופיע מסך אישור לעריכת התיאור ואישור השמירה.
4. חוזרים על שלב 3 לכל המוצרים על אותו מדף (ללא צורך לסרוק את ה-QR שוב).
5. **סיים מיקום** — כשעוברים למדף הבא, וחוזרים לשלב 2.
6. **שמור Excel** — בכל שלב, בוחרים היכן לשמור את קובץ ה-Excel המעודכן.

## חשוב לדעת
- כל הנתונים (כולל מיקומים שכבר אושרו) נשמרים מקומית בטלפון (SQLite) לאורך
  כל הדרך — אם האפליקציה נסגרת בטעות, אין צורך להתחיל מחדש; רק ה"מיקום
  הנוכחי" הפעיל יתאפס ויהיה צורך לסרוק אותו שוב.
- טעינת קובץ Excel **חדש** מוחקת את מצב הסריקה הקודם (מתחילה עבודה חדשה).
- האפליקציה קוראת רק את הגיליון (sheet) הראשון בקובץ.
- אין צורך באינטרנט בשום שלב — הסריקה (ML Kit) וקריאת/כתיבת ה-Excel
  מתבצעות לגמרי על המכשיר.
