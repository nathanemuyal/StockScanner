<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Exposes the app's private files dir (where the two working Excel
         files live) through the FileProvider, so they can be shared/exported
         out of the app (e.g. to Drive, email, or "Save to device") without
         ever needing broad storage permissions. -->
    <files-path name="working_files" path="." />
</paths>
